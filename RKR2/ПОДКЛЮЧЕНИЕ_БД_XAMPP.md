# Подключение базы данных в XAMPP

## Подробное руководство по настройке и подключению MySQL базы данных для интернет-магазина

---

## Содержание

1. [Установка и запуск XAMPP](#установка-и-запуск-xampp)
2. [Создание базы данных](#создание-базы-данных)
3. [Импорт структуры базы данных](#импорт-структуры-базы-данных)
4. [Настройка подключения в PHP](#настройка-подключения-в-php)
5. [Проверка подключения](#проверка-подключения)
6. [Решение возможных проблем](#решение-возможных-проблем)

---

## Установка и запуск XAMPP

### Шаг 1: Скачивание XAMPP

1. Перейдите на официальный сайт: https://www.apachefriends.org/
2. Скачайте версию XAMPP для вашей операционной системы (Windows)
3. Установите XAMPP в папку `C:\xampp` (рекомендуется)

### Шаг 2: Запуск сервисов

1. Откройте **XAMPP Control Panel**
2. Запустите следующие модули:
   - **Apache** (для работы PHP)
   - **MySQL** (для работы с базой данных)

   Нажмите кнопку **Start** рядом с каждым модулем. При успешном запуске индикатор должен стать зеленым.

### Шаг 3: Проверка работы

- Откройте браузер и перейдите по адресу: `http://localhost`
- Должна открыться страница приветствия XAMPP
- Для работы с базой данных перейдите: `http://localhost/phpmyadmin`

---

## Создание базы данных

### Способ 1: Через phpMyAdmin (рекомендуется)

1. Откройте браузер и перейдите на `http://localhost/phpmyadmin`
2. В левом меню нажмите на вкладку **"Новая"** (New) или **"Создать базу данных"**
3. В поле **"Имя базы данных"** введите: `shop_db`
4. В выпадающем списке **"Сравнение"** выберите: `utf8mb4_unicode_ci`
5. Нажмите кнопку **"Создать"**

### Способ 2: Через SQL запрос

1. В phpMyAdmin выберите вкладку **"SQL"**
2. Введите следующий запрос:

```sql
CREATE DATABASE shop_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

3. Нажмите кнопку **"Вперед"** (Go)

---

## Импорт структуры базы данных

### Шаг 1: Подготовка SQL файла

В проекте уже есть файл `database.sql`, который содержит:
- Структуру всех таблиц
- Связи между таблицами (foreign keys)
- Тестовые данные

### Шаг 2: Импорт через phpMyAdmin

1. В phpMyAdmin выберите созданную базу данных `shop_db` (кликните на неё в левом меню)
2. Перейдите на вкладку **"Импорт"** (Import)
3. Нажмите кнопку **"Выбрать файл"** (Choose File)
4. Выберите файл `database.sql` из папки проекта
5. Убедитесь, что выбран формат **SQL**
6. Нажмите кнопку **"Вперед"** (Go) внизу страницы

### Шаг 3: Проверка импорта

После успешного импорта вы должны увидеть:
- Таблицы: `users`, `categories`, `products`, `wishlist`, `feedback`
- Сообщение об успешном выполнении запросов

Проверьте наличие таблиц в левом меню phpMyAdmin.

---

## Настройка подключения в PHP

### Файл config.php

В проекте уже создан файл `config.php` с настройками подключения. Рассмотрим его структуру:

```php
// Параметры подключения к базе данных
define('DB_HOST', 'localhost');        // Хост базы данных
define('DB_USER', 'root');             // Имя пользователя MySQL
define('DB_PASS', '');                 // Пароль MySQL (по умолчанию пустой)
define('DB_NAME', 'shop_db');          // Имя базы данных
```

### Объяснение параметров:

1. **DB_HOST** (`localhost`)
   - Адрес сервера базы данных
   - Для XAMPP всегда `localhost`
   - Если MySQL работает на другом порту, используйте `localhost:3307`

2. **DB_USER** (`root`)
   - Имя пользователя MySQL
   - По умолчанию в XAMPP используется `root`
   - Можно создать отдельного пользователя для безопасности

3. **DB_PASS** (пустая строка `''`)
   - Пароль пользователя MySQL
   - По умолчанию в XAMPP пароль пустой
   - Если вы установили пароль, укажите его здесь

4. **DB_NAME** (`shop_db`)
   - Имя созданной базы данных
   - Должно совпадать с именем базы, созданной в phpMyAdmin

### Функция подключения

```php
function getDBConnection() {
    // Создаем подключение к базе данных
    $conn = new mysqli(DB_HOST, DB_USER, DB_PASS, DB_NAME);
    
    // Проверяем подключение
    if ($conn->connect_error) {
        die("Ошибка подключения к базе данных: " . $conn->connect_error);
    }
    
    // Устанавливаем кодировку UTF-8
    $conn->set_charset("utf8mb4");
    
    return $conn;
}
```

**Что делает эта функция:**
- Создает новое подключение к MySQL используя класс `mysqli`
- Проверяет успешность подключения
- Устанавливает кодировку UTF-8 для корректной работы с русским языком
- Возвращает объект подключения

### Использование подключения

В любом PHP файле проекта подключение используется так:

```php
require_once 'config.php';

$conn = getDBConnection();

// Выполнение SQL запроса
$result = $conn->query("SELECT * FROM products");

// Закрытие подключения
$conn->close();
```

---

## Проверка подключения

### Способ 1: Создание тестового файла

Создайте файл `test_connection.php` в корне проекта:

```php
<?php
require_once 'config.php';

echo "<h2>Тест подключения к базе данных</h2>";

try {
    $conn = getDBConnection();
    echo "<p style='color: green;'>✓ Подключение успешно!</p>";
    
    // Проверка таблиц
    $tables = $conn->query("SHOW TABLES");
    echo "<h3>Найденные таблицы:</h3><ul>";
    while ($row = $tables->fetch_array()) {
        echo "<li>" . $row[0] . "</li>";
    }
    echo "</ul>";
    
    // Проверка количества товаров
    $products = $conn->query("SELECT COUNT(*) as count FROM products");
    $count = $products->fetch_assoc();
    echo "<p>Товаров в базе: <strong>" . $count['count'] . "</strong></p>";
    
    $conn->close();
} catch (Exception $e) {
    echo "<p style='color: red;'>✗ Ошибка: " . $e->getMessage() . "</p>";
}
?>
```

Откройте в браузере: `http://localhost/RKR2/test_connection.php`

### Способ 2: Проверка через основной сайт

1. Запустите Apache и MySQL в XAMPP
2. Откройте в браузере: `http://localhost/RKR2/index.php`
3. Если страница загружается без ошибок - подключение работает

---

## Решение возможных проблем

### Проблема 1: "Access denied for user 'root'@'localhost'"

**Причина:** Неверный пароль или имя пользователя

**Решение:**
1. Откройте phpMyAdmin
2. Перейдите на вкладку **"Учетные записи пользователей"** (User accounts)
3. Проверьте пароль для пользователя `root`
4. Если пароль установлен, обновите его в `config.php`:
   ```php
   define('DB_PASS', 'ваш_пароль');
   ```

### Проблема 2: "Unknown database 'shop_db'"

**Причина:** База данных не создана

**Решение:**
1. Создайте базу данных через phpMyAdmin (см. раздел "Создание базы данных")
2. Убедитесь, что имя базы данных точно совпадает с `DB_NAME` в `config.php`

### Проблема 3: "Can't connect to MySQL server"

**Причина:** MySQL не запущен

**Решение:**
1. Откройте XAMPP Control Panel
2. Убедитесь, что MySQL запущен (зеленый индикатор)
3. Если не запускается, проверьте, не занят ли порт 3306 другим приложением

### Проблема 4: Кракозябры вместо русского текста

**Причина:** Неправильная кодировка

**Решение:**
1. Убедитесь, что в `config.php` есть строка:
   ```php
   $conn->set_charset("utf8mb4");
   ```
2. Проверьте, что база данных создана с кодировкой `utf8mb4_unicode_ci`
3. Убедитесь, что все HTML страницы имеют:
   ```html
   <meta charset="UTF-8">
   ```

### Проблема 5: "Call to undefined function mysqli_connect()"

**Причина:** Расширение mysqli не включено в PHP

**Решение:**
1. Откройте файл `C:\xampp\php\php.ini`
2. Найдите строку: `;extension=mysqli`
3. Уберите точку с запятой: `extension=mysqli`
4. Перезапустите Apache в XAMPP

---

## Структура базы данных

### Таблица `users` (Пользователи)
- `id` - уникальный идентификатор
- `username` - имя пользователя
- `email` - электронная почта
- `password` - хешированный пароль
- `created_at` - дата регистрации

### Таблица `categories` (Категории)
- `id` - уникальный идентификатор
- `name` - название категории
- `description` - описание категории

### Таблица `products` (Товары)
- `id` - уникальный идентификатор
- `category_id` - ссылка на категорию
- `name` - название товара
- `description` - описание товара
- `price` - цена
- `image` - путь к изображению
- `stock` - количество в наличии
- `specifications` - характеристики товара
- `created_at` - дата добавления

### Таблица `wishlist` (Список покупок)
- `id` - уникальный идентификатор
- `user_id` - ссылка на пользователя
- `product_id` - ссылка на товар
- `added_at` - дата добавления

### Таблица `feedback` (Обратная связь)
- `id` - уникальный идентификатор
- `user_id` - ссылка на пользователя (может быть NULL)
- `name` - имя отправителя
- `email` - email отправителя
- `message` - текст сообщения
- `created_at` - дата отправки

---

## Безопасность

### Рекомендации для продакшена:

1. **Создайте отдельного пользователя БД:**
   ```sql
   CREATE USER 'shop_user'@'localhost' IDENTIFIED BY 'strong_password';
   GRANT ALL PRIVILEGES ON shop_db.* TO 'shop_user'@'localhost';
   FLUSH PRIVILEGES;
   ```

2. **Используйте подготовленные запросы** (уже реализовано в проекте):
   ```php
   $stmt = $conn->prepare("SELECT * FROM users WHERE id = ?");
   $stmt->bind_param("i", $user_id);
   ```

3. **Храните пароли в хешированном виде** (используется `password_hash()`)

4. **Не выводите ошибки БД пользователям** в продакшене

---

## Дополнительные ресурсы

- [Документация MySQL](https://dev.mysql.com/doc/)
- [Документация PHP mysqli](https://www.php.net/manual/ru/book.mysqli.php)
- [XAMPP официальный сайт](https://www.apachefriends.org/)
- [phpMyAdmin документация](https://www.phpmyadmin.net/docs/)

---

## Быстрая проверка

После выполнения всех шагов выполните:

1. ✓ XAMPP запущен (Apache и MySQL)
2. ✓ База данных `shop_db` создана
3. ✓ Файл `database.sql` импортирован
4. ✓ Файл `config.php` настроен правильно
5. ✓ Тестовый файл `test_connection.php` работает

Если все пункты выполнены - база данных успешно подключена! 🎉

---

**Автор:** Система автоматизации  
**Дата:** 2024  
**Версия:** 1.0

